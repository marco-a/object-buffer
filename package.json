{
  "name": "object-buffer",
  "version": "0.0.4",
  "description": "A simple library to buffer properties on an object.",
  "main": "dist/ObjectBuffer.js",
  "author": "Marco A.",
  "license": "MIT",
  "url": "https://github.com/marco-a/object-buffer",
  "dependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-istanbul": "^0.12.1",
    "babel-preset-es2015": "^6.22.0",
    "jasmine": "^2.5.3",
    "jasmine-spec-reporter": "^3.2.0",
    "nyc": "^10.1.2"
  },
  "scripts": {
    "compile": "rm -rf dist && babel src --out-dir=dist",
    "test": "babel-node spec/run.js",
    "coverage": "babel-istanbul cover spec/run.js",
    "example": "babel-node src/example.js"
  }
}
